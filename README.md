# opentender.com.au

The OpenTender public site, served by GitHub Pages. Source of truth is
`site/` in the (private) tender_agency repo; deploy with
`scripts/deploy-site.sh` from there. Edits made directly here get
overwritten on the next deploy.
